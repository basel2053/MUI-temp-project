import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    error: {
      main: '#FF695F',
    },
    primary: {
      main: '#03A4ED',
    },
  },
});

export default theme;
